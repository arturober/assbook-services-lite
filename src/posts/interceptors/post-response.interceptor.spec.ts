import { PostResponseInterceptor } from './post-response.interceptor';

describe('PostResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new PostResponseInterceptor()).toBeDefined();
  });
});
